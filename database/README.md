# Database

Database-related code lives in these existing locations:

Schema and API contracts:
- `shared/schema.ts`
- `shared/routes.ts`

Drizzle and migrations:
- `drizzle.config.ts`
- `drizzle/`

Database connection and persistence:
- `server/db.ts`
- `server/storage.ts`

Helper scripts:
- `script/seed-billing.mjs`
